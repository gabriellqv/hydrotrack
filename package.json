{
  "name": "hydrotrack",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma de monitoramento de telemetria hidrica IoT",
  "scripts": {
    "prepare": "husky",
    "lint": "cd backend && vendor/bin/pint --test && cd ../frontend && npm run lint && npm run format:check",
    "test": "cd backend && php artisan test && cd ../frontend && npm run test:unit -- --run",
    "format": "cd backend && vendor/bin/pint && cd ../frontend && npm run format"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^20.5.3",
    "commitlint": "^20.5.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  }
}
