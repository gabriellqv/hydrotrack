# Pipeline de Integração Contínua — HydroTrack
#
# Executado automaticamente em todo push e pull request para a branch develop.
# Roda lint (Pint + ESLint + Prettier) e testes (Pest) para garantir
# que o código mergeado esteja sempre correto e padronizado.

name: CI

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [develop, main]
    paths:
      - "backend/**"
      - "frontend/**"
      - ".github/workflows/ci.yml"
      - "docker-compose.yml"
  pull_request:
    branches: [develop, main]
    paths:
      - "backend/**"
      - "frontend/**"
      - ".github/workflows/ci.yml"
      - "docker-compose.yml"

jobs:
  # ─────────────────────────────────────────────
  # Detecção de arquivos modificados
  # ─────────────────────────────────────────────
  changes:
    name: Detectar mudanças
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Filtrar paths modificados
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - "backend/**"
              - ".github/workflows/ci.yml"
              - "docker-compose.yml"
            frontend:
              - "frontend/**"
              - ".github/workflows/ci.yml"

  # ─────────────────────────────────────────────
  # Backend: lint (Pint) + testes (Pest)
  # ─────────────────────────────────────────────
  backend:
    name: Backend (Pint + Pest)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: changes
    if: ${{ needs.changes.outputs.backend == 'true' }}
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          extensions: mbstring, sqlite3, pdo_sqlite
          coverage: none

      - name: Cache do Composer
        uses: actions/cache@v4
        with:
          path: backend/vendor
          key: composer-${{ hashFiles('backend/composer.lock') }}
          restore-keys: composer-

      - name: Instalar dependências
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Copiar .env de testes
        run: cp .env.example .env

      - name: Gerar chave da aplicação
        run: php artisan key:generate

      - name: Verificar code style (Pint)
        run: vendor/bin/pint --test

      - name: Verificar vulnerabilidades em dependencias (Composer Audit)
        run: composer audit --no-dev --locked
        continue-on-error: true

      - name: Executar testes (Pest)
        run: vendor/bin/pest --colors=always

  # ─────────────────────────────────────────────
  # Frontend: lint (ESLint + Prettier) + type-check
  # ─────────────────────────────────────────────
  frontend:
    name: Frontend (Lint + Type Check)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: changes
    if: ${{ needs.changes.outputs.frontend == 'true' }}
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependências
        run: npm ci

      - name: Verificar formatação (Prettier)
        run: npm run format:check

      - name: Verificar lint (ESLint)
        run: npm run lint

      - name: Verificar tipos (TypeScript)
        run: npm run type-check

      - name: Verificar vulnerabilidades em dependencias (npm audit)
        run: npm audit --audit-level=high
        continue-on-error: true

      - name: Executar testes unitários
        run: npm run test:unit -- --run

      - name: Build de produção
        run: npm run build

      - name: Upload do artifact do build
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist
